# Optional. The client already defaults to this URL in development.
VITE_API_URL=http://localhost:5000/api

# Optional. Production builds use browser-local demo data by default so every
# visitor gets a private fresh prototype session. Set this to false only when
# you want a deployed frontend to share the API above.
# VITE_TASKIFY_DEMO_MODE=false
